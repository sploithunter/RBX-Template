## Summary

<!-- What changed and why. -->

## Domain

- [ ] Template (reusable infrastructure)
- [ ] Game (Pet Realm)

## Checklist

- [ ] `mise run ci` passes (selene + StyLua + rojo build + headless)
- [ ] Wiki updated if meaningful (dated `docs/wiki/LOG.md` entry; relevant page)
- [ ] Branch named by domain (`template/*`, `game/*` / `pet-realm/*`, `agent/*`)
- [ ] Shared files (`.mise.toml`, `default.project.json`, wiki `LOG`/`CURRENT_STATUS`) touched only as needed — append-only where applicable

## Notes for other agents

<!-- Anything the template / game / delegated agents should know (e.g. a
     template improvement surfaced here, a follow-up issue filed, a breaking
     change to a shared contract). -->
